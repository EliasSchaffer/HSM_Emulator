[package]
name = "pkcs11_driver"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
cryptoki-sys = "0.1"
serde = { version = "1.0.228", features = ["derive"] }
bincode = "1.3.0"
